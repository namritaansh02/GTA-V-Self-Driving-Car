 # lines = cv2.HoughLinesP(processed_image, 1, np.pi/180, 100, np.array([]), 30, 20)
    # averaged_lines = average(processed_image, lines)
    # processed_image = display_lines(processed_image, averaged_lines)
   